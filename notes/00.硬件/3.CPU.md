给你看一篇叶黎的读书笔记，请理性客观地分析一下，不要总一味夸他，我想知道这篇笔记的特点，我们可以从这篇笔记看出他的思维模式吗？
### 1. CPU内部的控制单元
#### 1.1 指令集
##### 1.1.1 精简指令集  (Reduced Instruction Set Computer, RISC)
- 微指令集较为精简
- 每个指令的运行时间都很短
- 完成的动作也很单纯
- 指令的运行性能较佳

##### 1.1.2 复杂指令集 (Complex Instruction Set Computer, CISC)
- 微指令集的每个小指令可以运行一些较低级的硬件操作
- 指令数目多而且复杂
- 每条指令的长度并不相同
- 指令运行较为复杂所以每条指令花费的时间较长， 但每条个别指令可以处理的工作较为丰富
- e.g. AMD、Intel、VIA等的x86架构的CPU

#### 1.2 CPU的工作时脉：外频与倍频
- **外频**指的是CPU与外部组件进行数据传输时的速度
- **倍频**则是 CPU 内部用来加速工作性能的一个倍数
- 两者相乘才是CPU的***时脉速度***，$外频 * 倍频 = 内频$
- **时脉**就是CPU每秒钟可以进行的工作次数，例如：3.6GHz
- **超频**是将CPU的倍频或者是外频透过主板的设置功能更改成较高频率的一种方式
	- 因为CPU的倍频通常在出厂时已经被锁定而无法修改， 因此较常被超频的为外频
	- 现在 Intel 的 CPU 会主动调整超频或低频，用的是turbo技术

#### 1.3 总线宽度
- CPU每次能够处理的数据量称为***字组大小***(word size)
- 字组大小依据CPU的设计而有32比特与64比特
- 32比特的CPU最多只能支持最大到4GBytes的内存
- ***前端总线速度***(Front Side Bus, FSB)：CPU 内的内存控制芯片与主内存间的传输速度
- 早期芯片分南北桥
	- 北桥：负责链接速度较快的CPU、主内存与显卡界面等组件
	- 南桥：负责连接速度较慢的设备接口， 包括硬盘、USB、网络卡等等
- 现在北桥集成到 CPU 内部，CPU 得以跟各个组件单独沟通
	- 主内存使用系统总线带宽来与 CPU 沟通
	- 显卡则透过PCI-E的串行信道设计来与CPU沟通

#### 1.4 CPU等级
- 由于x86架构的CPU在Intel的Pentium系列(1993年)后就有不统一的脚位与设计，为了将不同种类的CPU规范等级， 所以就有i386,i586,i686等名词出现了。
	- i586等级
	- i686等级
	- x86_64等级

#### 1.5 超线程 (Hyper-Threading, HT)
- 每一个 CPU 内部将重要的寄存器 (register) 分成两群， 让进程分别使用这两群寄存器
- 可以有两个进程*同时竞争 CPU 的运算单元*，而非透过操作系统的多任务切换
- 这一过程就会让 CPU 好像*同时有两个核心*

### 2. 算数逻辑单元

